stages:          
  - build
  - lint_format
  - test
  - deploy

build-job:       
  stage: build
  before_script:
    - cp ${ENV} .env
    - cp ${ENV_TEST} .env-test
  script:
    - docker build -t lume-api-image .

Lint: 
  stage: lint_format
  script: 
  - docker run --rm --network LumeNetwork lume-api-image ruff check 

Format:
  stage: lint_format
  script: 
  - docker run --rm --network LumeNetwork lume-api-image ruff format --check

Tests:
  stage: test
  script: 
  - docker run --rm --network LumeNetwork lume-api-image pytest -v

deploy-job:
  stage: deploy  
  before_script:
  script:
    - docker compose -f docker-compose-ci.yml up --build -d
    - docker exec lume_nginx nginx -s reload 
